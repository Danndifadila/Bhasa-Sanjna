// An array to store translation data (currently empty)
const translations = [
    // Add your translation objects here, e.g., { key: 'hello', value: 'Hola' }
];

// Export the translations array for use in other modules
export default translations;
